# Paths that never belong in the design-system repo (effectory-ux/Engage-Design-system-).
# Used by sync-design-system.sh. Prototypes stay in this repo only.

/.git
/.git/**
.DS_Store
/dist
/node_modules

# Prototypes: the whole folder plus the loose prototype pages at the root.
/prototypes
/novanta-*.html
/team-it-*.html
/before-*.html
/after-*.html
/action-center-*.html
/ac-overview-embed.html
/conversation-guide*.html

# Local tooling that only makes sense in this working copy.
/.claude/launch.json
/.ds-sync-exclude
/sync-design-system.sh
